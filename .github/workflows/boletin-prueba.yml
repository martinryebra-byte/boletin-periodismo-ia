name: Boletín Prueba

on:
  workflow_dispatch:

jobs:
  test-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install newspaper3k
          pip install "lxml[html_clean]"

      - name: Run agent (prueba)
        run: python agent.py
